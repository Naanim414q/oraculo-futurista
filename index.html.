<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Oráculo Futurista</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-cyan-400 flex flex-col items-center justify-center min-h-screen font-mono">
  
  <h1 class="text-3xl font-bold mb-6 animate-pulse">🔮 Oráculo Futurista del Año 3000</h1>
  
  <textarea id="pregunta" placeholder="Escribe tu pregunta..." 
    class="w-80 p-3 rounded-lg bg-gray-900 border border-cyan-500 text-white mb-4"></textarea>
  
  <button onclick="predecir()" 
    class="bg-cyan-600 hover:bg-cyan-400 text-black px-6 py-2 rounded-lg shadow-lg transition">
    Consultar al Oráculo
  </button>
  
  <p id="respuesta" class="mt-6 text-lg italic text-center"></p>

  <script>
    function predecir() {
      const respuestas = [
        "En las ciudades flotantes del 2400 tu destino brillará entre constelaciones digitales.",
        "Tras la fusión solar, tu camino será guiado por inteligencias holográficas.",
        "Los túneles interestelares revelarán el eco de tu nombre en la eternidad.",
        "Cuando la humanidad se fusione con la máquina, tu voz será código inmortal."
      ];
      const random = Math.floor(Math.random() * respuestas.length);
      document.getElementById('respuesta').innerText = respuestas[random];
    }
  </script>

</body>
</html>
